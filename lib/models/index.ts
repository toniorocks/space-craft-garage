/**
 * lib/models — single entry point for all data-access models.
 *
 * Usage:
 *   import { findAllSpaceCrafts, createUser } from "@/lib/models";
 */

export * from "./spacecraft.model";
export * from "./user.model";
